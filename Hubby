for i, v in pairs(workspace.Collectables:GetDescendants()) do
    if v.Name == "Collider" then
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.CFrame
        wait(1)
    end
end
